// Stub for Next.js' `server-only` package, used by vitest. The package itself
// throws if imported into a client bundle; under tests there is no bundler, so
// this empty module is enough to satisfy the import.
export {};
